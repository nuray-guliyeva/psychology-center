spring:
  application:
    name: psych-center-backend

  datasource:
    url: jdbc:postgresql://localhost:5433/psych_center
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
